from graphics import *
import math

num_sides = int(input("Number of sides: "))
radius = int(input("Radius: "))

win = GraphWin("Rotated Polygon", 600, 600)
center = Point(300, 300)

vertices = []
angle_step = 360 / num_sides

for i in range(num_sides):
    angle_rad = math.radians(i * angle_step)
    x = center.getX() + radius * math.cos(angle_rad)
    y = center.getY() + radius * math.sin(angle_rad)
    vertices.append(Point(x, y))

rotated = []
for p in vertices:
    dx = p.getX() - center.getX()
    dy = p.getY() - center.getY()
    new_dx = dy
    new_dy = -dx
    rotated.append(Point(center.getX() + new_dx, center.getY() + new_dy))

polygon = Polygon(rotated)
polygon.draw(win)

win.getMouse()
win.close()
